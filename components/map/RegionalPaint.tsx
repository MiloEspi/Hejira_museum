"use client";

export default function RegionalPaint() {
  // Regional paint is handled directly in HejiraMap via react-simple-maps Geography components
  // with watercolor filter applied. This component is reserved for additional
  // decorative paint overlays if needed in the future.
  return null;
}
