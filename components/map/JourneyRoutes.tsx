"use client";

export default function JourneyRoutes() {
  // Journey routes are rendered directly in HejiraMap using react-simple-maps <Line> components
  // with journey-specific styles (color, dash pattern, width).
  // This component is reserved for the animated route reveal (§8.3) in a future phase.
  return null;
}
